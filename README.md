# Zurri Content Assets

Public image host for Zurri Foods Instagram content. Images placed under `posts/<date>/` are
referenced by their `raw.githubusercontent.com` URL as the `image_url` for Instagram Graph API
publish calls (Instagram's servers must be able to fetch images over plain public HTTP).

Not for sensitive content — anything committed here is publicly visible.
